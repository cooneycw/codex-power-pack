# Codex Power Pack - optional local environment
#
# This repository no longer owns MCP servers, Docker Compose runtime, or deploy
# entrypoints. Keep local secrets in your shell, credential manager, or external
# service-specific configuration.

# Optional for Woodpecker API-driven skills and local diagnostics.
WOODPECKER_SERVER=https://woodpecker.example.com
WOODPECKER_API_TOKEN=

# Optional for AWS-backed secrets workflows.
AWS_REGION=us-east-1
AWS_PROFILE=
