# Sample skill

A minimal, clean Codex skill. It carries no Claude-only construct, so a working
harness-lint must report a genuine pass over it - one markdown file examined.

Run the thing, then read what it printed.
