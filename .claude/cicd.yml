# .codex/cicd.yml - CI/CD configuration for Codex Power Pack
#
# Framework and package manager are auto-detected (Python + uv).
# This file configures quality gates and recommendations.

build:
  required_targets:
    - lint
    - test

  recommended_targets:
    - format
    - typecheck
    - verify
    - clean
