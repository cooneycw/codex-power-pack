interface:
  display_name: "Flow Auto"
  short_description: "Deliver an open GitHub issue through worktree, approval, PR, merge, CI, and optional deploy gates"
  default_prompt: "Use $flow-auto to deliver a specific open GitHub issue end to end; do not use it for questions or ordinary repository edits."
policy:
  allow_implicit_invocation: true
