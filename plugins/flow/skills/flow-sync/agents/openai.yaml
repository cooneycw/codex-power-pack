interface:
  display_name: "Flow Sync"
  short_description: "Commit and push issue-branch work so it can be resumed on another machine"
  default_prompt: "Use $flow-sync to publish the current issue branch for safe cross-machine pickup."
policy:
  allow_implicit_invocation: false
