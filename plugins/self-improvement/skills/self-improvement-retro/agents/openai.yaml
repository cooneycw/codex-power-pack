interface:
  display_name: "Self Improvement Retro"
  short_description: "Turn masked workflow-friction records into user-confirmed durable improvements"
  default_prompt: "Use $self-improvement-retro to review captured friction and propose confirmed workflow fixes."
policy:
  allow_implicit_invocation: false
