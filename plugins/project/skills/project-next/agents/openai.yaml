interface:
  display_name: "Project Next"
  short_description: "Recommend the next safe repository action from issue, PR, branch, worktree, review, and CI evidence"
  default_prompt: "Use $project-next to recommend what to do next in an existing repository; do not use it to scaffold or start implementation."
policy:
  allow_implicit_invocation: true
