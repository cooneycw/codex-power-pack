interface:
  display_name: "Project Init"
  short_description: "Create a new local Python project after explicit destination approval"
  default_prompt: "Use $project-init only when explicitly asked to create a new local Python project; do not use it for existing repositories, publication, Spec Kit, issue sync, or persistent configuration."
policy:
  allow_implicit_invocation: false
