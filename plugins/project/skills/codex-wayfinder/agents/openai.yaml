interface:
  display_name: "Codex Wayfinder"
  short_description: "Map long-horizon decisions across sessions"
  default_prompt: "Use $codex-wayfinder to chart an ambiguous multi-session effort or resume one decision from an existing map."
policy:
  allow_implicit_invocation: false
