interface:
  display_name: "Secrets Get"
  short_description: "Get credentials securely with masking"
  default_prompt: "Use $secrets-get when you need get credentials securely with masking"
policy:
  allow_implicit_invocation: false
