interface:
  display_name: "Secrets Run"
  short_description: "Run one command with project secrets injected only into its subprocess environment"
  default_prompt: "Use $secrets-run to execute this command with masked project credentials injected into the subprocess."
policy:
  allow_implicit_invocation: false
