interface:
  display_name: "QA Test"
  short_description: "Test a web application through Playwright MCP and report reproducible findings"
  default_prompt: "Use $qa-test to exercise this web page with Playwright MCP and report reproducible failures."
policy:
  allow_implicit_invocation: false
